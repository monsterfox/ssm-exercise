<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.foxbill.mapper.UserMapper">
<!--<mapper namespace="test">-->
<!--    <select id="findAllUser" resultType="com.foxbill.pojo.User">
        select * from t_user
    </select>

    <insert id="addUser">
        insert into t_user values(null ,#{username},#{sex},#{age});
    </insert>-->
    
    <update id="uptUser">
        update t_user set username = #{username},sex = #{sex},age = #{age} where id = #{id}
    </update>

    <delete id="delUser">
        delete from t_user where id = #{id}
    </delete>
</mapper>